apiVersion: v1
kind: Service
metadata:
  name: hutao-pet-api
  namespace: hutao-pet
spec:
  type: ClusterIP
  selector:
    app: hutao-pet-api
  ports:
    - name: http
      port: 80
      targetPort: http
