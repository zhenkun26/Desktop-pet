apiVersion: v1
kind: ConfigMap
metadata:
  name: hutao-pet-config
  namespace: hutao-pet
data:
  # 非敏感运行配置；敏感值一律放 Secret
  APP_ENV: "production"
  LOG_LEVEL: "info"
  API_TIMEOUT_MS: "15000"
  DB_MAX_CONNECTIONS: "10"
